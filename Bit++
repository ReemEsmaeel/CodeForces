#include <iostream>
using namespace std;

int main () {
   int n,X;
   X = 0;
   cin >> n;
   string statement;
   for (int i=0; i<n; i++){
       cin >> statement;
       if (statement == "++X" || statement == "X++")
           X++;
       else X--;
   }
   cout << X;
   return 0;
}
